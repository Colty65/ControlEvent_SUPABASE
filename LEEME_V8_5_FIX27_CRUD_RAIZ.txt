ControlEvent v8.5_prod FIX27 - CRUD RAIZ

Correccion real del fallo detectado:
- Los parches v46 instalaban listeners en WINDOW CAPTURE y se ejecutaban antes del CRUD anterior, borrando solo en memoria.
- Ahora el CRUD explicito se instala tambien en WINDOW CAPTURE desde el HEAD, antes de los parches legacy.
- Los botones Alta/Modificar/Baja quedan interceptados antes que cualquier listener antiguo.
- Baja de compra: DELETE /api/crud/compras/:id y luego recarga /api/state; si el registro sigue en BBDD se muestra error.
- PUT /api/state sigue bloqueado para guardados normales.

Prueba recomendada:
1) Evento Finalizado: cambiar a En curso.
2) COMPRAS: eliminar una linea.
3) Verificar que no aparece error y que tras recarga sigue borrada.
4) Finalizar evento, salir y entrar.

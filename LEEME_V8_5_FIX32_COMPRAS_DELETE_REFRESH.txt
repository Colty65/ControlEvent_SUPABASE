ControlEvent v8.5_prod FIX32 - COMPRAS DELETE REFRESH

Esta version parte de FIX31.

No cambia la BBDD ni las RPC SQL.

Corrige solo la baja visual de COMPRAS:
- El DELETE por RPC ya funcionaba y borraba en ce_compras.
- La pantalla quedaba con una copia vieja hasta cambiar de evento.
- Ahora, tras confirmar el DELETE en BBDD, se elimina la fila de memoria/pantalla inmediatamente.
- Despues se relee /api/state con no-cache y se repinta COMPRAS/RESUMEN/DONACIONES.
- Si una cache vieja devuelve aun la fila, se vuelve a consultar y se mantiene eliminada en memoria para impedir un segundo clic sobre el mismo registro.

Mantener global_write_lock = ON.

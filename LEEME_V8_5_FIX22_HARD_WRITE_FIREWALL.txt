ControlEvent v8.5_prod FIX22 - HARD WRITE FIREWALL

Objetivo: cortar por lo sano cualquier escritura automatica que no venga de un CRUD explicito y auditado.

Cambios:
1) PUT /api/state queda bloqueado para guardados normales. Solo lectura para login/refresco/cambio de evento/cambio de ventana/render/globos/fotos.
2) Solo se acepta PUT /api/state con __forceReplaceAll si ademas trae cabecera X-ControlEvent-Backup-Restore: 1.
3) /api/crud-deltas queda bloqueado.
4) DELETE /api/crud queda bloqueado temporalmente para impedir bajas indirectas.
5) Se carga v8-5-hard-write-firewall.js en el HEAD, antes del resto de scripts.
6) saveState/pushStateToServer quedan neutralizados para servidor y solo persisten localmente.

Consecuencia deliberada:
- Esta version prioriza NO perder datos. Algunas altas/modificaciones legacy que dependan de /api/state pueden no persistir hasta reimplementar CRUD boton-a-boton.
- Las bajas quedan bloqueadas temporalmente.

Siguiente paso correcto:
- Rehacer mantenimientos uno a uno con endpoints CRUD explicitos: INGRESOS, COMPRAS, DONACIONES, DOCUMENTOS, EVENTOS, PERSONAS, TIENDAS, PRODUCTOS.

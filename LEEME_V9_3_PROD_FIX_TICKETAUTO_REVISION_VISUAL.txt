ControlEvent v9.3_prod - FIX TicketAuto revisión visual y Gemini

Cambios incluidos:

1) Gemini / IA
- La ruta /api/receipt-ai/analyze ya devuelve proveedor/modelo/código de error cuando falla, sin exponer claves.
- Si hay GEMINI_API_KEY, OPENIA_API_KEY, GOOGLE_API_KEY, CONTROLEVENT_GEMINI_API_KEY o GOOGLE_GENERATIVE_AI_API_KEY, la llamada se fuerza a Gemini antes que OpenAI.
- OPENAI_API_KEY solo se usa como OpenAI si parece una clave OpenAI real (empieza por sk-). Si OPENAI_API_KEY contiene una clave Gemini, se interpreta como Gemini.
- Gemini prueba modelos en cascada si el primero falla por cuota/modelo/límite:
  gemini-2.0-flash-lite, gemini-2.0-flash, gemini-1.5-flash, gemini-1.5-flash-8b.
- El error visible en pantalla ahora permite distinguir si se está llamando a Gemini y con qué modelo.

2) Ventana TicketAuto
- Ventana más grande.
- Foto del ticket siempre visible a la izquierda, en tamaño grande.
- Botón para ampliar la foto del ticket a pantalla casi completa.
- Datos y líneas de factura a la derecha.

3) Líneas y total factura
- Importe se calcula automáticamente como Unid. x Precio.
- El campo Importe queda de solo lectura para evitar fórmulas visibles tipo =Unid.*Precio.
- Añadido Total factura líneas OK.
- Si la IA lee el total del ticket, se muestra Total leído en foto por IA y Diferencia.

4) Ordenación
- Las líneas reconocidas se ordenan como quedan en TKxx/COMPRAS: por producto, manteniendo blancos al final.

5) Cache de navegador
- Cambiado el parámetro de carga del JS de TicketAuto para forzar que Vercel/navegador cojan el fichero nuevo.

/* ControlEvent v4_0_exp · ITV INTÉRPRETE GEMINI · laboratorio aislado */
(function(){
  'use strict';
  if(window.__ceZuzuInterpreterLab)return;window.__ceZuzuInterpreterLab=true;
  const $=id=>document.getElementById(id),text=v=>v==null?'':String(v),trim=v=>text(v).trim(),num=v=>Number(v)||0,arr=v=>Array.isArray(v)?v:[],esc=v=>text(v).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
  let preview=null,rows=[],summary=null,controller=null,running=false,openedAt='';
  function storedAuth(){for(const key of ['ControlEvent_v4_0_exp_login_user','ControlEvent_ce_acceso_usuario','ControlEvent_auth_user_v509']){try{const raw=sessionStorage.getItem(key)||localStorage.getItem(key);if(raw)return JSON.parse(raw);}catch(_){}}return null;}
  function auth(){return window.__CE_ZUZU_ITV_LOGIN_USER__||window.ControlEventApp?.authUser||window.ControlEventRuntime?.app?.authUser||window.authUser||window.__CONTROL_EVENT_USER__||storedAuth()||{};}
  function role(){const u=auth();return trim(u?.nivel||u?.Nivel||u?.NIVEL||u?.rol||u?.Rol).toUpperCase()||'GD';}
  function headers(){const u=auth();return{'Content-Type':'application/json','X-ControlEvent-Feature':'zuzu-interpreter-lab-v1','X-ControlEvent-Actor':encodeURIComponent(JSON.stringify({nivel:role(),identificacion:text(u.identificacion||u.Identificacion),nombre:text(u.nombre||u.Nombre)}))};}
  function style(){if($('ceZuzuInterpreterStyle'))return;const s=document.createElement('style');s.id='ceZuzuInterpreterStyle';s.textContent=`
    #ceZuzuInterpreterOverlay{position:fixed;inset:0;z-index:2147483647;background:rgba(15,23,42,.42);display:flex;align-items:center;justify-content:center;padding:14px}
    #ceZuzuInterpreterOverlay *{box-sizing:border-box}.zil-modal{width:min(1540px,calc(100vw - 28px));height:min(930px,calc(100vh - 28px));background:#fff;border:2px solid #7c3aed;border-radius:18px;box-shadow:0 24px 80px rgba(15,23,42,.38);display:flex;flex-direction:column;overflow:hidden;color:#0f172a}
    .zil-head{display:flex;align-items:center;gap:10px;padding:12px 16px;background:#312e81;color:#fff}.zil-head h2{font-size:18px;margin:0}.zil-head small{opacity:.88}.zil-spacer{flex:1}.zil-head button,.zil-controls button{border:0;border-radius:9px;padding:8px 11px;font-weight:850;cursor:pointer}.zil-head button{background:#fff;color:#312e81}.zil-head button.close{background:#fee2e2;color:#991b1b}
    .zil-info{padding:10px 14px;background:#f5f3ff;border-bottom:1px solid #ddd6fe;display:flex;gap:8px;flex-wrap:wrap;align-items:center}.zil-pill{padding:5px 8px;border-radius:999px;background:#fff;border:1px solid #c4b5fd;font-size:11px;font-weight:750}.zil-note{font-size:11px;color:#5b21b6;font-weight:750}
    .zil-controls{padding:9px 14px;border-bottom:1px solid #e2e8f0;display:flex;gap:8px;align-items:center}.zil-controls button.start{background:#16a34a;color:#fff}.zil-controls button.stop{background:#dc2626;color:#fff}.zil-controls button.download{background:#0284c7;color:#fff}.zil-controls input{width:80px;padding:6px;border:1px solid #cbd5e1;border-radius:8px}.zil-controls .phase{margin-left:auto;font-size:11px;font-weight:750;color:#475569;max-width:48%;text-align:right}
    .zil-metrics{display:grid;grid-template-columns:repeat(7,minmax(0,1fr));gap:7px;padding:9px 14px;background:#f8fafc;border-bottom:1px solid #e2e8f0}.zil-metric{padding:8px;border:1px solid #e2e8f0;border-radius:10px;background:#fff;text-align:center}.zil-metric b{display:block;font-size:17px}.zil-metric span{font-size:9px;color:#64748b;font-weight:850}.zil-metric.good b{color:#15803d}.zil-metric.bad b{color:#b91c1c}
    .zil-results{flex:1;overflow:auto;padding:10px 14px}.zil-table{width:100%;border-collapse:separate;border-spacing:0;font-size:10px}.zil-table th{position:sticky;top:0;background:#ede9fe;color:#312e81;padding:7px;border-bottom:1px solid #c4b5fd;text-align:left;z-index:2}.zil-table td{padding:7px;border-bottom:1px solid #e2e8f0;vertical-align:top}.zil-table tr.ko{background:#fff1f2}.zil-table tr.ok{background:#f0fdf4}.zil-status{font-weight:950}.zil-status.ok{color:#15803d}.zil-status.ko{color:#b91c1c}.zil-plan{white-space:pre-wrap;font-family:ui-monospace,Consolas,monospace;font-size:9px;max-width:440px;word-break:break-word}.zil-reasons{color:#991b1b}.zil-progress{height:5px;background:#e2e8f0}.zil-progress>div{height:100%;width:0;background:#7c3aed;transition:width .15s}.zil-empty{padding:28px;text-align:center;color:#64748b}
  `;document.head.appendChild(s);}
  function modal(){return `<div id="ceZuzuInterpreterOverlay"><div class="zil-modal"><div class="zil-head"><h2>🧠 ITV · INTÉRPRETE GEMINI</h2><small>Gemini interpreta · CE NO se ejecuta · SIN function calling</small><div class="zil-spacer"></div><button id="zilDownload">⬇ JSON</button><button id="zilClose" class="close">✕ CERRAR</button></div><div class="zil-info" id="zilInfo"><span class="zil-pill">Preparando batería…</span></div><div class="zil-controls"><label>Casos <input id="zilMax" type="number" min="1" max="90" value="90"></label><button class="start" id="zilStart">▶ EJECUTAR</button><button class="stop" id="zilStop">■ DETENER</button><span class="phase" id="zilPhase">30 escenarios × 3 repeticiones. Solo se mide interpretación.</span></div><div class="zil-progress"><div id="zilBar"></div></div><div class="zil-metrics"><div class="zil-metric"><b id="zilDone">0/0</b><span>DECISIONES</span></div><div class="zil-metric good"><b id="zilJson">0%</b><span>JSON VÁLIDO</span></div><div class="zil-metric good"><b id="zilKnown">0%</b><span>CAPACIDAD CONOCIDA</span></div><div class="zil-metric good"><b id="zilExec">0%</b><span>EJECUTABLE</span></div><div class="zil-metric good"><b id="zilSemantic">0%</b><span>SEMÁNTICA</span></div><div class="zil-metric"><b id="zilTokens">0</b><span>TOKENS</span></div><div class="zil-metric"><b id="zilCost">0,0000 €</b><span>COSTE</span></div></div><div class="zil-results" id="zilResults"><div class="zil-empty">Pulsa EJECUTAR. Ningún módulo de ControlEvent será llamado por esta batería.</div></div></div></div>`;}
  async function open(){style();$('ceZuzuInterpreterOverlay')?.remove();document.body.insertAdjacentHTML('beforeend',modal());openedAt=new Date().toISOString();$('zilClose').onclick=close;$('ceZuzuInterpreterOverlay').onclick=e=>{if(e.target.id==='ceZuzuInterpreterOverlay')close();};$('zilStart').onclick=run;$('zilStop').onclick=stop;$('zilDownload').onclick=download;await loadPreview();}
  function close(){stop();$('ceZuzuInterpreterOverlay')?.remove();}
  async function loadPreview(){try{const res=await fetch('/api/zuzu-tests/interpreter-preview',{headers:headers(),cache:'no-store'}),d=await res.json();if(!res.ok)throw new Error(d.error||`HTTP ${res.status}`);preview=d;renderInfo();}catch(e){$('zilPhase').textContent='No se pudo preparar la batería: '+(e.message||e);}}
  function renderInfo(){if(!preview)return;$('zilInfo').innerHTML=[`Batería: ${preview.batteryCode}`,`Base: ${preview.baseCases} escenarios`,`Repeticiones: ${preview.repeats}`,`Total: ${preview.total}`,`Modelo: ${preview.model}`,`CE ejecutado: NO`,`Function calling: NO`].map(x=>`<span class="zil-pill">${esc(x)}</span>`).join('')+`<span class="zil-note">El catálogo CE solo se lee para saber qué capacidades existen y validar el JSON.</span>`;}
  function pct(n,d){return d?Math.round(n*1000/d)/10:0;}function sumNow(){const d=rows.length;return{done:d,json:rows.filter(r=>r.metrics?.jsonValid).length,known:rows.filter(r=>r.metrics?.capabilityKnown).length,exec:rows.filter(r=>r.metrics?.executable).length,semantic:rows.filter(r=>r.metrics?.semanticCorrect).length,tokens:rows.reduce((a,r)=>a+num(r.usage?.totalTokens),0),cost:rows.reduce((a,r)=>a+num(r.usage?.costEur),0)};}
  function renderMetrics(){const s=summary||sumNow(),done=num(s.done),total=num(s.total)||num(preview?.total)||90,json=s.jsonValid!=null?num(s.jsonValid):num(s.json),known=s.capabilityKnown!=null?num(s.capabilityKnown):num(s.known),ex=s.executable!=null?num(s.executable):num(s.exec),sem=s.semanticCorrect!=null?num(s.semanticCorrect):num(s.semantic);$('zilDone').textContent=`${done}/${total}`;$('zilJson').textContent=(s.jsonValidPct!=null?s.jsonValidPct:pct(json,done))+'%';$('zilKnown').textContent=(s.capabilityKnownPct!=null?s.capabilityKnownPct:pct(known,done))+'%';$('zilExec').textContent=(s.executablePct!=null?s.executablePct:pct(ex,done))+'%';$('zilSemantic').textContent=(s.semanticCorrectPct!=null?s.semanticCorrectPct:pct(sem,done))+'%';$('zilTokens').textContent=new Intl.NumberFormat('es-ES').format(num(s.tokens));$('zilCost').textContent=new Intl.NumberFormat('es-ES',{minimumFractionDigits:4,maximumFractionDigits:4}).format(num(s.costEur))+' €';$('zilBar').style.width=(total?Math.min(100,done*100/total):0)+'%';}
  function renderRows(){if(!rows.length){$('zilResults').innerHTML='<div class="zil-empty">Sin resultados todavía.</div>';return;}$('zilResults').innerHTML=`<table class="zil-table"><thead><tr><th>#</th><th>Grupo</th><th>Usuario</th><th>Plan Gemini</th><th>Validación</th><th>Resultado</th></tr></thead><tbody>${rows.map((r,i)=>{const m=r.metrics||{},checks=[['JSON',m.jsonValid],['CAP',m.capabilityKnown],['EXEC',m.executable],['SEM',m.semanticCorrect]].map(([k,v])=>`${v?'✅':'❌'} ${k}`).join(' · ');return`<tr class="${trim(r.status).toLowerCase()}"><td>${i+1}<br><small>${esc(r.baseId||'')} · R${num(r.repeat)||1}</small></td><td>${esc(r.category||'')}</td><td><b>${esc(r.prompt||'')}</b></td><td class="zil-plan">${esc(JSON.stringify(r.plan||{},null,2))}</td><td>${checks}${arr(r.reasons).length?`<div class="zil-reasons">${arr(r.reasons).map(esc).join('<br>')}</div>`:''}</td><td><span class="zil-status ${trim(r.status).toLowerCase()}">${esc(r.status||'KO')}</span><br><small>${num(r.durationMs)} ms · ${num(r.usage?.totalTokens)} tok</small></td></tr>`;}).join('')}</tbody></table>`;}
  async function run(){if(running)return;if(!preview)await loadPreview();if(!preview)return;rows=[];summary=null;renderRows();renderMetrics();controller=new AbortController();running=true;$('zilPhase').textContent='Ejecutando decisiones independientes. CE sigue sin ejecutarse…';const max=Math.max(1,Math.min(90,num($('zilMax').value)||90));try{const res=await fetch('/api/zuzu-tests/interpreter-run-stream',{method:'POST',headers:headers(),signal:controller.signal,body:JSON.stringify({maxCases:max})});if(!res.ok){let d={};try{d=await res.json();}catch(_){}throw new Error(d.error||`HTTP ${res.status}`);}const reader=res.body.getReader(),dec=new TextDecoder();let buf='';while(true){const {done,value}=await reader.read();if(done)break;buf+=dec.decode(value,{stream:true});let p;while((p=buf.indexOf('\n'))>=0){const line=buf.slice(0,p).trim();buf=buf.slice(p+1);if(line)handle(JSON.parse(line));}}if(buf.trim())handle(JSON.parse(buf.trim()));}catch(e){if(e.name!=='AbortError')$('zilPhase').textContent='Error: '+(e.message||e);}finally{running=false;controller=null;renderMetrics();}}
  function handle(msg){if(msg.type==='progress'){$('zilPhase').textContent=`${msg.index}/${msg.total} · ${msg.prompt||''}`;return;}if(msg.type==='case'){rows.push(msg.case);renderMetrics();renderRows();return;}if(msg.type==='summary'){summary=msg.summary;renderMetrics();renderRows();$('zilPhase').textContent=`Terminada · semántica ${summary.semanticCorrectPct}% · ejecutable ${summary.executablePct}% · JSON ${summary.jsonValidPct}% · CE ejecutado: NO`;return;}if(msg.type==='error')$('zilPhase').textContent='Error: '+msg.error;}
  function stop(){if(controller){try{controller.abort();}catch(_){}}controller=null;running=false;}
  function download(){const payload={type:'ControlEvent ITV INTERPRETER GEMINI',reportFormat:'INTERPRETER-LAB-V1',version:'v4_0_exp',batteryCode:preview?.batteryCode||'INTERPRETER-GEMINI-30X3',exportedAt:new Date().toISOString(),openedAt,preview,summary:summary||sumNow(),results:rows};const blob=new Blob([JSON.stringify(payload,null,2)],{type:'application/json'}),a=document.createElement('a'),stamp=new Date().toISOString().replace(/[:.]/g,'-');a.href=URL.createObjectURL(blob);a.download=`ControlEvent_v4_0_exp_ITV_Zuzu_${stamp}-interpreter-gemini-90.json`;document.body.appendChild(a);a.click();setTimeout(()=>{URL.revokeObjectURL(a.href);a.remove();},1000);}
  window.ceOpenZuzuInterpreterLab=open;
})();

# BANK4_21 - Memory / alias / unit stability

- Explicit recall waits for persistent memory evidence before semantic compilation.
- Structured target_ref is normalized without `[object Object]`.
- DB social aliases have priority but are unioned with configured aliases during migration.
- Alias ambiguity is authoritative before ordinary person fuzzy matching.
- Screen unit guard removes EUR from count/quantity phrases and protects ordinal tokens such as 4ºs.
- Thinking murmur is slower and more sustained.

const fs=require('fs');
const voice=fs.readFileSync('public/app/features/v22-voz3-zuzu.js','utf8');
const svc=fs.readFileSync('services/event-ai.service.js','utf8');
const tts=fs.readFileSync('services/zuzu-voice.service.js','utf8');
const html=fs.readFileSync('public/index.html','utf8');
let ok=0,ko=0;const t=(c,m)=>{if(c){ok++;console.log('OK · '+m)}else{ko++;console.error('KO · '+m)}};
t(/FIX13-UNIFIED-VOICE-BUS-V60/.test(voice),'build FIX13 activo');
t(/cloudFallback:true/.test(voice),'Voz CE es el transporte de micro predeterminado');
t(/function startAmbient[\s\S]{0,450}startCloudRecognition\('ambient'/.test(voice),'wake ambiental usa Voz CE');
t(/function startUser[\s\S]{0,900}startCloudRecognition\('user'/.test(voice),'turnos hablados usan Voz CE');
t(/getUserMedia\(cloudAudioConstraints\(\)\)/.test(voice)&&/MediaRecorder/.test(voice),'entrada estándar es getUserMedia + MediaRecorder');
t(/wake:j\.wake===true/.test(voice)&&/wakeHint\|\|hasWake\(text\)/.test(voice),'wake usa la decisión semántica del transcriptor CE y no solo texto literal');
t(/function neuralVoiceName\(\)\{return 'Algenib';\}/.test(voice),'una voz neural estándar fija');
t(/function neuralPlaybackRate\(\)\{return 1\.10;\}/.test(voice),'ritmo estándar ágil 1.10x');
t(/neuralSpeechFailure/.test(voice)&&/no usaré otra voz/.test(voice),'fallo neural no cambia a voz del navegador');
t(!/speechSynthesis\.speak|new SpeechSynthesisUtterance/.test(voice),'el módulo de conversación no puede emitir voz del navegador');
t(/microseñales de espera quedan SOLO en pantalla/.test(voice),'frases de espera no disparan segunda voz');
t(/function speakLocalControl[\s\S]{0,350}setStatus\(text,'ok'\)/.test(voice),'controles locales no usan voz del navegador');
t(/kind==='barge'/.test(voice)&&/startCloudBargeUtterance/.test(voice),'barge-in permanece en Voz CE');
t(/echoCancellation:true,noiseSuppression:true,autoGainControl:true/.test(voice),'captura estándar mantiene cancelación de eco');
t(/vnextVoiceNumberBudget/.test(svc)&&/PRESUPUESTO DE CIFRAS EXACTAS/.test(svc),'narración oral tiene presupuesto numérico');
t(/return 0;\n\}/.test(svc)&&/return 1;/.test(svc),'por defecto 0 cifras y 1 solo en petición cuantitativa');
t(/PODA ORAL/.test(svc)&&/vnextVoiceNeedsLeanRewrite/.test(svc),'si el narrador se excede hay reescritura oral');
t(/No recites en una misma respuesta ingresos \+ compras \+ donaciones \+ saldo \+ asistencia/.test(svc),'prohibido el inventario hablado');
t(/sin céntimos|céntimos/.test(svc),'modo oral conserva regla sin céntimos');
t(/Math\.round\(a\)===Math\.round\(n\)/.test(voice),'cliente acepta redondeo certificado al euro');
t(/FIX13-UNIFIED-VOICE-BUS-V60/.test(html),'cache-buster FIX13 cargado');
t(/gemini-2\.5-flash-preview-tts/.test(tts),'TTS neural estándar sigue en servidor');
console.log(`\nFIX13 unified voice bus: ${ok} OK · ${ko} KO`);process.exitCode=ko?1:0;
